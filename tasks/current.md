# 当前任务清单

> 历史已完成任务见 `tasks/completed.md`

## 当前待办

### TASK-309: 后台移动端导航与表格响应式修复（P1）
- 修复 `app/jack/layout.tsx` 侧边导航在移动端横向挤压问题（支持换行/纵向折叠/可滚动）。
- 修复后台列表页在小屏下可用性：`posts` / `announcements` / `subscribers` 表格避免操作区溢出，保证核心操作可点击。
- 保持桌面端信息密度不下降，移动端优先保证可读与可操作。

### TASK-310: 全站间距与布局体系统一（P1/P2）
- 统一前台页面容器策略（`safe-x`、`max-w-*`、`container` 的使用规则），消除页面切换时左右留白跳变。
- 修复 `components/post-form.tsx` 中封面图输入区在移动端一行拥挤的问题（改为断点换行或纵向布局）。
- 复查并收敛关键页面的 `padding`、`gap`、段落节奏，确保 4px 网格一致性。

### TASK-311: 视觉与交互一致性收敛（P2/P3）
- 对齐 `project-docs/08-design-system.md`：收敛过度渐变、超大圆角与不一致阴影，统一控件视觉层级。
- 修正搜索弹窗说明与行为不一致问题：实现上下键选择/回车打开，或移除错误提示文案。
- 评估并处理全局 `overflow-x` 策略，避免“隐藏溢出”掩盖真实布局问题。

### TASK-312: 高质量长文内容串行导入（P1）
- 执行策略：每次会话只导入 1 篇长文（1500+ 字），完成后再登记下一篇。
- TASK-312-01（已完成）：
- 主题：前端性能深水区（INP + 主线程调度 + 长任务切片）。
- 结果：使用 `scripts/import-advanced-post-task-001.js` 清理旧短文并导入 1 篇长文。
- TASK-312-02（下一篇）：
- 主题：大型 React 应用中的状态分层与一致性模型（Server State / UI State / Form State / URL State）。
- 要求：包含架构图思路、反模式清单、迁移步骤、代码片段与回归检查表。

## 已搁置（未来可能重启）

- **TASK-301**: 相关文章推荐
- **TASK-302**: 文章点赞 / 收藏系统
- **TASK-304**: 结构化数据 SEO（JSON-LD）
- **TASK-306**: 图片懒加载 + 点击放大（Lightbox）
- **TASK-307**: 404 页面推荐文章
- **TASK-308**: 键盘快捷键支持

## 本期明确不做

- 多用户博客 / 访客注册登录
- 文章付费墙 / 会员系统
- 实时 WebSocket 聊天或评论区自研
- 国际化 i18n
- 独立的移动端 App 或 PWA
