# 当前任务清单

## 已完成任务（二期）

### TASK-101: 文章目录 TOC
- **状态**: ✅ 已完成
- **描述**: 文章详情页右侧增加悬浮目录（≥1280px 显示），支持点击平滑滚动、IntersectionObserver 高亮当前章节。

### TASK-102: 代码块复制按钮
- **状态**: ✅ 已完成
- **描述**: 改造 `MarkdownRenderer`，在代码块右上角增加「复制」按钮，点击后显示「已复制」状态 2 秒。

### TASK-103: Giscus 评论系统
- **状态**: ✅ 已完成
- **描述**: 文章页底部接入 Giscus 评论组件。未配置环境变量时显示友好提示块，告知用户如何启用。

### TASK-104: 关于页 / 友链页 / 归档页
- **状态**: ✅ 已完成
- **描述**:
  - `about`：个人简介与联系方式
  - `friends`：友情链接卡片列表
  - `archive`：按年份分组的文章时间轴

### TASK-105: 阅读时间 + 上下篇导航 + 阅读量统计
- **状态**: ✅ 已完成
- **描述**:
  - 文章页顶部显示阅读时间（按 500 字/分钟估算）
  - 文章底部增加「上一篇 / 下一篇」导航卡片
  - 每次访问文章自动 `views + 1`，并在文章页和卡片上展示

### TASK-106: 导航栏与 404 页面
- **状态**: ✅ 已完成
- **描述**: Header 增加「归档、友链、关于」入口；新增中文 404 页面。

### TASK-107: 首页纯文字列表改版
- **状态**: ✅ 已完成
- **描述**: 移除 PostCard 封面图，改为单列纯文字列表布局，避免无图时的灰底尴尬；标题加大、摘要 line-clamp-2、元信息横排，提升高级感。

### TASK-108: Slug 自动生成（拼音混合）
- **状态**: ✅ 已完成
- **描述**: 引入 `pinyin-pro`，根据标题自动生成拼音混合 slug（中文转拼音、英文/数字保留）；后台表单移除 Slug 手动输入，改为只读展示；API 层自动去重（尾部递增 `-2`、`-3`）。

## 已完成任务（三期：用户体验与内容分发）

### TASK-201: 头像系统（Avatar）
- **优先级**: 高
- **状态**: ✅ 已完成
- **描述**:
  - 在 `User` 模型中复用/增强 `image` 字段，支持管理员在后台上传自定义头像。
  - 若未上传，使用 Gravatar 作为 fallback（基于 email 的 MD5 哈希）。
  - 头像展示位置：Header 用户菜单、文章页作者署名区、关于页、OpenGraph 动态图（后续 TASK-206）。
- **验收标准**:
  - [x] 后台 `/admin/profile` 或设置页可上传头像，支持裁剪预览。
  - [x] 头像文件保存到 `public/uploads/avatars/`（或 OSS，本期优先本地）。
  - [x] 文章页底部增加「作者简介卡片」，展示头像、昵称、一句话简介。
  - [x] 未上传头像时，自动请求 Gravatar；Gravatar 不存在时回退到默认占位图。

### TASK-202: 站点通知系统（Site Notifications）
- **优先级**: 高
- **状态**: ✅ 已完成
- **描述**:
  - 提供两种通知形态：**全局顶部横幅（Banner）** 和 **文章内嵌提示块（Callout）**。
  - Banner 用于站点级公告（如「本站即将维护」），支持关闭（`localStorage` 记录已关闭的 ID，24h 内不再显示）。
  - 文章内嵌 Callout 用于文章级更新说明（如「2026-04-20 更新：修正了某处错误」），在 Markdown 中通过 `:::note` / `:::warning` 语法渲染（使用 remark 插件或自定义解析）。
- **验收标准**:
  - [x] 新增 `Announcement` 模型（`id`, `content`, `type: banner|toast`, `link?`, `isActive`, `startAt`, `endAt`, `createdAt`）。
  - [x] 管理后台新增「公告管理」页面，可增删改查、设置有效期。
  - [x] 前端 Layout 自动拉取活跃 Banner，渲染在 Header 下方；用户点击关闭后写入 `localStorage`。
  - [x] `MarkdownRenderer` 支持 `:::note` / `:::warning` / `:::tip` 语法，渲染为带图标的提示块。

### TASK-203: RSS 订阅增强
- **优先级**: 高
- **状态**: ✅ 已完成
- **描述**:
  - 已有 `/feed.xml` 路由，但需补全 Atom 标准字段、文章摘要、分类标签。
  - 在站点 Footer 和文章页显眼位置增加 RSS 订阅入口（图标 + 链接）。
  - 可选补充 JSON Feed (`/feed.json`)，方便现代阅读器订阅。
- **验收标准**:
  - [x] `feed.xml` 包含 `<author>`、`<category>`、`<guid>`、`<pubDate>`、`<description>`（excerpt 或前 200 字）。
  - [x] 文章详情页和 Footer 增加 RSS 图标链接。
  - [x] 新增 `/feed.json` 路由，输出 JSON Feed v1 格式。
  - [x] 通过 RSS 校验工具（如 validator.w3.org/feed）无报错。

### TASK-204: 文章搜索（全文 + 快捷键）
- **优先级**: 高
- **状态**: ✅ 已完成
- **描述**:
  - 首页 / 文章列表页增加搜索框，支持按标题、摘要、标签搜索。
  - 支持 `Cmd/Ctrl + K` 唤起搜索（参考 Algolia DocSearch 的模态框交互）。
  - 由于使用 SQLite，搜索采用 Prisma `contains` 模糊查询（标题+摘要+标签联合查询），无需引入 Elasticsearch。
- **验收标准**:
  - [x] 首页顶部或 Header 中集成搜索入口。
  - [x] 按下 `Cmd/Ctrl + K` 弹出搜索模态框，输入即时过滤文章列表。
  - [x] 搜索结果展示标题、摘要高亮（匹配关键字加粗）、发布日期。
  - [x] 空状态友好提示「未找到相关文章」。

### TASK-205: 文章系列 / 专栏（Series）
- **优先级**: 中
- **状态**: ✅ 已完成
- **描述**:
  - 现代技术博客（如 GitHub Blog、CSS-Tricks）普遍支持「系列文章」，方便读者按顺序阅读。
  - 例如「从零构建博客」系列包含 5 篇文章，文章页顶部展示系列目录，标记当前阅读进度。
- **验收标准**:
  - [x] 新增 `Series` 模型（`id`, `name`, `slug`, `description`），与 `Post` 建立多对一关系，并增加 `seriesOrder` 字段排序。
  - [x] 后台编辑文章时可选择所属系列及在系列中的顺序。
  - [x] 文章页顶部若属于某个系列，渲染系列导航组件：展示系列名称 + 该系列下所有文章列表，当前文章高亮。

### TASK-206: OpenGraph 动态图生成
- **优先级**: 中
- **状态**: ✅ 已完成
- **描述**:
  - 为每篇文章自动生成带标题、作者、站点品牌的社交分享图（1200×630）。
  - 使用 `@vercel/og` 或 `satori` 在 Edge Runtime 动态生成 SVG → PNG。
- **验收标准**:
  - [x] 访问 `/api/og?title=xxx` 返回 PNG 图片。
  - [x] 文章详情页 `metadata.openGraph.images` 指向该动态图。
  - [x] OG 图包含文章标题、作者名、站点 Logo/头像、品牌色背景。

### TASK-207: 置顶文章（Pinned Post）
- **优先级**: 中
- **状态**: ✅ 已完成
- **描述**:
  - 后台可勾选「置顶」，置顶文章在首页列表最上方显示，并带「置顶」徽标。
- **验收标准**:
  - [x] `Post` 模型新增 `pinned` Boolean 字段（默认 `false`）。
  - [x] 首页查询按 `pinned DESC, createdAt DESC` 排序。
  - [x] 置顶文章卡片左上角显示「置顶」标签。

### TASK-208: Newsletter / 邮件订阅
- **优先级**: 中
- **状态**: ✅ 已完成
- **描述**:
  - 在 Footer 或文章页底部增加邮件订阅框，读者可提交邮箱接收新文章通知。
  - 由于项目目前无邮件服务，本期先做数据收集，后续对接 Resend / SendGrid 发送。
- **验收标准**:
  - [x] 新增 `Subscriber` 模型（`id`, `email@unique`, `subscribedAt`, `isActive`）。
  - [x] 前端提供邮箱输入框，提交后显示「订阅成功」Toast。
  - [x] 后台增加「订阅者管理」页面，可查看列表、手动取消订阅。
  - [x] 接口层做邮箱格式校验与重复提交处理。

### TASK-209: Markdown 剪贴板直接粘贴上传
- **优先级**: 低
- **状态**: ✅ 已完成
- **描述**:
  - 在后台 Markdown 编辑器中，支持截图后直接 `Ctrl+V` 粘贴上传图片，自动插入 Markdown 图片语法。
- **验收标准**:
  - [x] 监听编辑器 `paste` 事件，识别图片 Blob。
  - [x] 上传至 `public/uploads/` 并返回可访问 URL。
  - [x] 上传过程中在光标处显示占位符，成功后替换为 `![alt](url)`。

### TASK-210: 文章自动保存草稿
- **优先级**: 低
- **状态**: ✅ 已完成
- **描述**:
  - 后台编辑文章时，每 30 秒自动将当前内容保存到 `localStorage`；页面刷新或意外关闭后可恢复。
- **验收标准**:
  - [x] 编辑页 `useEffect` 定时将表单数据序列化到 `localStorage`（key: `draft-post-<postId|new>`）。
  - [x] 进入编辑页时检测 localStorage，若有较新的草稿，提示「恢复自动保存的草稿？」
  - [x] 用户手动保存或发布后，清除对应 localStorage 草稿。

---

## 已完成任务（四期）

### TASK-303: 阅读进度条 + 回到顶部
- **优先级**: 中
- **状态**: ✅ 已完成
- **描述**:
  - 文章页顶部增加细长的阅读进度条（随滚动百分比变化）。
  - 右下角悬浮「回到顶部」按钮，滚动超过 300px 时显示。
- **验收标准**:
  - [x] 进度条固定在 `Header` 下方或页面最顶部，`width` 随滚动百分比变化。
  - [x] 右下角固定位置显示回到顶部按钮，点击平滑滚动到顶部。
  - [x] 动画流畅，不引起布局偏移。

### TASK-305: 站点访问统计 Dashboard（管理员）
- **优先级**: 低
- **状态**: ✅ 已完成
- **描述**:
  - 管理后台增加简单的数据看板：总文章数、总阅读量、近 7 天访问量趋势、最受欢迎文章 TOP 5。
  - 由于无第三方统计服务，本期基于 `views` 字段聚合 + 简单日志表实现。
- **验收标准**:
  - [x] 新增 `PageView` 模型（`id`, `path`, `viewedAt`, `referrer?`, `ua?`）用于记录访问日志（异步写入，不阻塞渲染）。
  - [x] 后台 `/admin/dashboard` 展示关键指标卡片与趋势图（纯 CSS 条形图）。
  - [x] TOP 5 文章列表按 `views` 排序。

---

## 已搁置任务（暂不做）

### TASK-301: 相关文章推荐
- **优先级**: 中
- **状态**: ⏸️ 已搁置
- **描述**: 文章页底部根据标签匹配度推荐 3 篇相关文章。

### TASK-302: 文章点赞 / 收藏系统
- **优先级**: 中
- **状态**: ⏸️ 已搁置
- **描述**: 个人博客边际收益低，暂不实现。

### TASK-304: 结构化数据 SEO（JSON-LD）
- **优先级**: 中
- **状态**: ⏸️ 已搁置
- **描述**: 在文章页注入 `application/ld+json` 结构化数据。

### TASK-306: 图片懒加载 + 点击放大（Lightbox）
- **优先级**: 低
- **状态**: ⏸️ 已搁置
- **描述**: `MarkdownRenderer` 图片懒加载 + Lightbox。

### TASK-307: 404 页面推荐文章
- **优先级**: 低
- **状态**: ⏸️ 已搁置
- **描述**: 404 页面增加热门文章推荐。

### TASK-308: 键盘快捷键支持
- **优先级**: 低
- **状态**: ⏸️ 已搁置
- **描述**: 全局键盘快捷键（`j/k`、搜索、主题切换等）。

---

## 本期明确不做（已记录，防止范围蔓延）

- 多用户博客 / 访客注册登录（保持单管理员模式）。
- 文章付费墙 / 会员系统。
- 实时 WebSocket 聊天或评论区自研（已用 Giscus 满足）。
- 国际化 i18n（当前仅中文）。
- 独立的移动端 App 或 PWA（未来可考虑）。

---

## 任务优先级速览（推荐开发顺序）

**三期（近期）:**
1. **TASK-201** 头像系统
2. **TASK-203** RSS 订阅增强
3. **TASK-202** 站点通知系统
4. **TASK-204** 文章搜索
5. **TASK-207** 置顶文章
6. **TASK-206** OpenGraph 动态图
7. **TASK-205** 文章系列 / 专栏
8. **TASK-208** Newsletter 邮件订阅
9. **TASK-209 / 210** 粘贴上传 & 自动保存草稿

**四期（中期）:**
10. **TASK-301** 相关文章推荐
11. **TASK-302** 点赞系统
12. **TASK-303** 阅读进度条 + 回到顶部
13. **TASK-304** JSON-LD SEO
14. **TASK-305** 访问统计 Dashboard
15. **TASK-306** 图片懒加载 + Lightbox
16. **TASK-307** 404 推荐文章
17. **TASK-308** 键盘快捷键
